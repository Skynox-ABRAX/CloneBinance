export class footerItem{

        title: string;
        list: string [];

        constructor(titre :string, liste:string[]){

            this.title = titre;
            this.list = liste;
        }
}